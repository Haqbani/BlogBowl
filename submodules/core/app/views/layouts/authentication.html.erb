<!DOCTYPE html>
<html lang="<%= I18n.locale %>" dir="<%= I18n.locale == :ar ? 'rtl' : 'ltr' %>">
  <%= render "layouts/common_head" %>

  <body class="h-screen w-screen flex items-center justify-center bg-base-100">
    <%= yield %>
  </body>
</html>
